# Local Development MongoDB Configuration
MONGODB_URI=mongodb+srv://dev_username:dev_password@cluster-dev.mongodb.net/inventory-tracker?retryWrites=true&w=majority
MONGODB_DB_NAME=inventory-tracker-dev

# Local Development Server Configuration
PORT=3000
NODE_ENV=development

# Development Tools
NGROK_AUTH_TOKEN=your_ngrok_token_here

# Instructions:
# 1. Copy this file to .env.local for local development
# 2. Replace dev_username and dev_password with your MongoDB Atlas credentials  
# 3. For production, use .env.production template with production credentials